/**
 * src/app/(main)/tai-lieu/layout.tsx
 * Layout cho section Tài liệu (Portal).
 */

export default function TaiLieuLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
